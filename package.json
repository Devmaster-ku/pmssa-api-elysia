{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio",
    "seed:role-permissions": "bun run src/seed-role-permissions.ts",
    "seed:department-users": "bun run src/seed-department-users.ts",
    "migrate:projects": "bun run src/migrate-projects.ts",
    "migrate:project-status-enum": "bun run src/migrate-project-status-enum.ts",
    "seed:projects": "bun run src/seed-projects.ts",
    "seed:project-details": "bun run src/seed-project-details.ts",
    "seed:provinces": "bun run src/seed-provinces.ts"
  },
  "dependencies": {
    "@elysiajs/bearer": "^1.4.3",
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/jwt": "^1.4.1",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.45.1",
    "elysia": "latest",
    "pg": "^8.20.0",
    "redis": "^5.11.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.9"
  },
  "module": "src/index.js"
}
